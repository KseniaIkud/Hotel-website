mixin benefit(img, alt, title, description, split)
    .benefit
        img(src= img, alt= alt).benefit__img
        .benefit__text
            .benefit__title= title
            .benefit__description= description
    - if (split === "yes") {
        .benefit__split
    - }